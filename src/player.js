class Player {
  constructor(id, token) {
    this.id = id;
    this.token = token;
    this.points = 0;
    this.myTurn = false
  };

  savePointsToStorage() {

  };

  retrievePointsFromStorage() {

  };
};
